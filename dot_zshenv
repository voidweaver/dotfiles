BREWPATH="/home/linuxbrew/.linuxbrew/bin/brew"
[[ ! ( -f $BREWPATH && -x $BREWPATH ) ]] || eval $($BREWPATH shellenv)

export -U MANPATH INFOPATH

export EDITOR='vim'
export -U PATH="$HOME/.local/bin:$HOME/.yarn/bin:$PATH"

